# Maintainer: Felipe Ndc (Vicyos) <felipendc10@gmail.com>
pkgname=vicyos-calamares-config-nord-git
_pkgname=vicyos-calamares-config-nord
_destname1="/calamares/"
_destname2="/etc/calamares/"
_licensedir="/usr/share/vicyos/licenses/"
pkgver=21.1.7
pkgrel=4
pkgdesc="calamares config for vicyos-nord"
arch=('any')
url="https://github.com/felipendc/vicyos-calamares-config-nord.git"
license=('GPL3')
makedepends=('git')
depends=()
replaces=($_pkgname)
conflicts=($_pkgname)
provides=("${pkgname}")
options=(!strip !emptydirs)
source=(${_pkgname}::"git+https://github.com/felipendc/${_pkgname}.git")
sha256sums=('SKIP')

package() {
	install -dm755 ${pkgdir}${_licensedir}${_pkgname}
	install -m644  ${srcdir}/${_pkgname}/LICENSE ${pkgdir}${_licensedir}${_pkgname}

	install -dm755 ${pkgdir}${_destname1}
	install -dm755 ${pkgdir}${_destname2}

	rm -r  ${srcdir}/${_pkgname}/.git
	rm -r  ${srcdir}/${_pkgname}/setup-git-v1.sh
	rm -r  ${srcdir}/${_pkgname}/git-v1.sh
	rm -r  ${srcdir}/${_pkgname}/LICENSE	
	cp -r  ${srcdir}/${_pkgname}${_destname1}* ${pkgdir}${_destname2}
}